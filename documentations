export PATH="/local/temporary/babayeva/miniconda3/bin:$PATH"

conda install cython matplotlib numpy scipy opencv pyyaml packaging yaml
conda install pytorch=0.4.1 cuda90 -c pytorch
#conda install -c pytorch torchvision 
conda install -c hcc pycocotools 
conda install faiss-gpu cudatoolkit=9.0 -c pytorch
